package com.sakura.chat.v2.business.main

import androidx.activity.ComponentActivity
import com.sakura.chat.databinding.DialogAboutBinding
import com.sakura.chat.v2.base.dialog.ArchDialog

class AboutDialog(activity: ComponentActivity) : ArchDialog<DialogAboutBinding>(activity) {
    override fun convertView(binding: DialogAboutBinding) {
    }
}